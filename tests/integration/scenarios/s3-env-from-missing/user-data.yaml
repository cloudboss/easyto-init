disable-services:
  - chrony
  - ssh
env-from:
  # This object doesn't exist and optional is false (default)
  # Should fail with an error
  - s3:
      bucket: missing-bucket
      key: non-existent-object.json
command:
  - /bin/sh
  - -c
  - |
    exec >/dev/ttyS0 2>&1
    echo "FAIL: Should not have reached here - expected S3 not found error"
