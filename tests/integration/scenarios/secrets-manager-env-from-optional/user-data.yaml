disable-services:
  - chrony
  - ssh
env-from:
  - secrets-manager:
      secret-id: non-existent/secret
      optional: true
command:
  - /bin/sh
  - -c
  - |
    exec >/dev/ttyS0 2>&1
    echo "PASS"
