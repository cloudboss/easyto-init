disable-services:
  - chrony
  - ssh
volumes:
  - template:
      content: "{{#section}}hello"
      variables:
        section: true
      mount:
        destination: /out.html
command:
  - /bin/sh
  - -c
  - |
    exec >/dev/ttyS0 2>&1
    echo "FAIL: Should not have reached here"
