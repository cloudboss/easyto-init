disable-services:
  - chrony
  - ssh
volumes:
  - template:
      content: |
        {{#broken
      mount:
        destination: /out.html
command:
  - /bin/sh
  - -c
  - |
    exec >/dev/ttyS0 2>&1
    echo "FAIL: Should not have reached here"
