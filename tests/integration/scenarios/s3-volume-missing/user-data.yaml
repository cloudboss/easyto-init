disable-services:
  - chrony
  - ssh
volumes:
  - s3:
      bucket: empty-bucket2
      key-prefix: non-existent/
      mount:
        destination: /config
command:
  - /bin/sh
  - -c
  - |
    exec >/dev/ttyS0 2>&1
    echo "FAIL: Should not have reached here"
